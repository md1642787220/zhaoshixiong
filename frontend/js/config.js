/* ============================================================
 * config.js - 全局配置
 * ============================================================ */

/**
 * 后端 API 地址：
 * - 同源部署（后端托管前端静态页）留空字符串即可
 * - 前后端分离部署时改为后端地址，如 'http://localhost:3000'
 */
export const API = '';

/* API 基础路径 */
export const API_BASE = '/api';
