# ===== shixiong 后端环境变量示例 =====
# 复制本文件为 .env 后填写实际值（.env 已被 git 忽略）

# 后端监听端口，默认 3000
PORT=3000

# Python PDF Worker 服务地址（实现 PDF 高级功能：基于 pypdf / PyMuPDF 等 6 个开源库）
# 未配置时，引擎类 PDF 功能将返回降级提示
PDF_WORKER_URL=http://pdf-worker:8000
# 转发超时（毫秒），默认 120000
PDF_WORKER_TIMEOUT=120000
