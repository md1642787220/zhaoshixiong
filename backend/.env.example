# ===== Helper 后端环境变量示例 =====
# 复制本文件为 .env 后填写实际值（.env 已被 git 忽略）

# Stirling-PDF 云服务器地址（部署 Stirling-PDF 后填写，不带末尾斜杠）
# 未配置时，24 个「引擎类」PDF 功能将返回本机降级提示
STIRLING_API_URL=http://你的服务器IP:8080

# Stirling 转发超时（毫秒），默认 60000
STIRLING_TIMEOUT=60000

# 后端监听端口，默认 3000
PORT=3000
